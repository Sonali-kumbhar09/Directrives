import { RenderCssHighDirective } from './render-css-high.directive';

describe('RenderCssHighDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderCssHighDirective();
    expect(directive).toBeTruthy();
  });
});


